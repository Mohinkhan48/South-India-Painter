/**
 * services/index.ts
 *
 * Central export point for API/service layer functions.
 *
 * This folder handles all external data fetching and integrations.
 *
 * Planned modules:
 *   - contactService.ts — send contact/quote form submissions
 *   - analyticsService.ts — Google Analytics / event tracking helpers
 *
 * Services will be added here as each feature is built.
 */

export {};
