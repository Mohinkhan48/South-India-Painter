/**
 * hooks/index.ts
 *
 * Central export point for custom React hooks.
 *
 * Planned hooks:
 *   - useScrollLock — prevent body scroll (used in mobile menu)
 *   - useMediaQuery — responsive breakpoint detection
 *   - useIntersectionObserver — trigger animations on scroll
 *   - useDebounce — debounce input values
 *   - useLocalStorage — typed localStorage wrapper
 *
 * Hooks will be added here as each feature is built.
 */

export {};
