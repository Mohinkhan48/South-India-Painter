/**
 * pages/ColourIdeasPage.tsx
 *
 * PLACEHOLDER — Colour inspiration and palette browsing page.
 *
 * Planned sections:
 *   - Colour ideas hero
 *   - Room filter (living room, bedroom, kitchen, etc.)
 *   - Colour palette grid
 *   - Trending colours
 *   - CTA to book a colour consultation
 */

export default function ColourIdeasPage() {
  return (
    <section className="section container">
      <h1 style={{ fontFamily: 'var(--font-display)', marginBottom: '1rem' }}>
        Colour Ideas
      </h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Colour ideas inspiration page placeholder — design coming in a separate phase.
      </p>
    </section>
  );
}
