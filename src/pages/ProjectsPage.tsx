/**
 * pages/ProjectsPage.tsx
 *
 * PLACEHOLDER — Project portfolio listing page.
 *
 * Planned sections:
 *   - Projects hero
 *   - Filter bar (by category)
 *   - Masonry / grid of project cards
 *   - Load more / pagination
 */

export default function ProjectsPage() {
  return (
    <section className="section container">
      <h1 style={{ fontFamily: 'var(--font-display)', marginBottom: '1rem' }}>
        Our Projects
      </h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Projects portfolio placeholder — design coming in a separate phase.
      </p>
    </section>
  );
}
