/**
 * pages/ContactPage.tsx
 *
 * PLACEHOLDER — Contact and quote request page.
 *
 * Planned sections:
 *   - Contact hero
 *   - Contact form (React Hook Form + validation)
 *   - Contact details (phone, WhatsApp, email, address)
 *   - Google Maps embed
 *   - Working hours
 */

export default function ContactPage() {
  return (
    <section className="section container">
      <h1 style={{ fontFamily: 'var(--font-display)', marginBottom: '1rem' }}>
        Contact Us
      </h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Contact page placeholder — form and map design coming in a separate phase.
      </p>
    </section>
  );
}
